[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # Astro 7 requires Node >=22.12; pin the major so Netlify's default
  # can never drift underneath the build.
  NODE_VERSION = "24"
