---
interface Props {
  lines: string[];
  size: number;
  lineHeight?: string;
}

const { lines, size, lineHeight = ".86" } = Astro.props;
const html = lines.join("<br />");
---

<div class="display-heading" style={`--dh-size:${size}px;--dh-lh:${lineHeight}`} set:html={html} />

<style>
  .display-heading {
    font: 800 var(--dh-size) / var(--dh-lh) Archivo, sans-serif;
    /* Not font-stretch — see ShellSectionHead. This one happens to survive
       minification today, because the var()s stop the minifier folding the
       shorthand, but it is one refactor away from breaking the same way. */
    font-variation-settings: "wdth" 62;
    text-transform: uppercase;
    letter-spacing: -0.015em;
    color: var(--cream);
  }

  @media (max-width: 700px) {
    .display-heading {
      font-size: clamp(40px, 14vw, var(--dh-size));
    }
  }
</style>
